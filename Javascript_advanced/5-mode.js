//Changing DOM with closure

function changeMode(size, weight, transform, background, color){
    let changeMode = changeMode();
}
const closure = (font-size, font-weight, text-transform, background-color, color);

function main (){
    let spooky = (9, bold, uppercase, pink, green)
    let darkMode = (12, bold, capitalize, black, white)
    let screamMode = (12, normal, lowercase, white, black)
}
<body>
<p>Welcome Holberton</p>
<button>Spooky</button>
<button>Dark Mode</button>
<button>Scream Mode</button>
</body>

